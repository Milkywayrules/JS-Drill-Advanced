import React from 'react';

interface Props {
  zxc?: unknown;
}

const App: React.FC<Props> = ({zxc}: Props) => {
  return (
    <div className="text-lg text-orange-500">
      Lacrime
    </div>
  );
};

export default App;
